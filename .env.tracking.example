# Tracking Configuration

# Google Analytics 4
# Get your GA ID from: https://analytics.google.com
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Tracking Settings
# Enable/disable tracking globally
NEXT_PUBLIC_TRACKING_ENABLED=true

# Enable/disable backend tracking (stores events in database)
NEXT_PUBLIC_TRACKING_BACKEND_ENABLED=true

# Optional: Custom tracking endpoint (if different from /api/tracking)
# NEXT_PUBLIC_TRACKING_ENDPOINT=/api/tracking

# Optional: Sampling rate (0-1)
# 1.0 = track 100% of events
# 0.5 = track 50% of events
# 0.1 = track 10% of events
# NEXT_PUBLIC_TRACKING_SAMPLING_RATE=1.0
